---
const {titulo, parrafo, conoce, color} = Astro.props;
---


<div class="bg-gray-50 rounded-2xl transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-lg">
    <div class={`h-56 flex items-center justify-center relative overflow-hidden ${color} rounded-t-2xl`}>
        <div class="absolute inset-0"></div>
           <slot />
    </div>
            <h1 class="font-serif font-bold text-blue-950 text-xl justify-self-start px-6 py-6">{titulo}</h1>
            <h2 class="text-start text-gray-700 px-6">{parrafo}</h2>
            <h3 class="text-blue-900 cursor-pointer hover:text-black my-3 px-6 py-4 flex justify-self-start">{conoce}
               <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2 mt-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </h3>
</div>